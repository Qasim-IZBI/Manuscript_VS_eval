% ============================================================
%  Section 9: Epistemic Uncertainty Analysis
%  H&E to Sirius Red Virtual Staining Paper
% ============================================================

\section{Epistemic Uncertainty Analysis}
\label{sec:uncertainty}

The scaling study~(Section~\ref{sec:results_scaling}) identifies how model capacity and
data volume jointly affect translation quality, but it treats each configuration as a
deterministic predictor.
In an unpaired setting, no ground-truth target constrains the generator: a model may
produce a stain that appears visually plausible yet does not faithfully reproduce the
underlying tissue structure, a hallucination that is indistinguishable from a correct
translation in a single forward pass.
This is a critical concern for clinical adoption, as a hallucinated collagen pattern
carries the same visual appearance as a true one.
We address this gap by training deep ensembles for the best-performing generator size
from each model family at the full training data volume, for reasons detailed in
Section~\ref{sec:ens_selection}, and constructing spatially resolved epistemic
uncertainty maps that flag regions of high inter-member disagreement, enabling
pathologists to review low-confidence areas with appropriate caution.

% -------------------------------------------------------
\subsection{Model Selection for Ensemble Analysis}
\label{sec:ens_selection}
% -------------------------------------------------------

For each of the six model families (CycleGAN, UNIT, MUNIT, DCLGAN, UVCGAN,
CycleDiffusion), we select the generator size (Small, Medium, or Large) trained on the
full training set (100\% data fraction) that achieves the lowest CPA MAE on the
held-out test set.
Fixing all families to the full data fraction ensures that differences in ensemble
uncertainty reflect architectural properties rather than the amount of training data
seen.
Generator sizes are ranked by CPA MAE alone, as it is the primary benchmark for model
evaluation in this study: it directly measures whether the virtual stain supports the
same biological quantification as the real stain, which is the final objective of the
translation~(Section~\ref{sec:metrics}).
Where two sizes are tied on CPA MAE, the lower FID serves as a secondary tiebreak.
\textcolor{red}{[TODO: insert table of selected configurations once results are
available. For each model family, report the winning size (Small / Medium / Large),
CPA MAE score, and CPA MAE rank; include FID, LPIPS, and patch-SSIM as supplementary
columns for completeness.]}

\paragraph{Ensemble training.}
For each selected configuration, ten models are trained independently from distinct
random seeds, using the identical hyperparameters and training data as the corresponding
scaling-study run.
The ensemble size of $K{=}10$ is motivated by convergence analysis showing that
calibration errors plateau at approximately ten members for deep ensembles in
biomedical image segmentation~\cite{siddiqui2024uncertainty}, and is consistent
with prior ensemble-based uncertainty work in virtual staining
applications~\cite{trustworthy_cell_ensemble2023}.

% -------------------------------------------------------
\subsection{Uncertainty Quantification}
\label{sec:unc_maps}
% -------------------------------------------------------

Given an ensemble of $K{=}10$ independently trained generators
$\{G^{(k)}_{A \to B}\}_{k=1}^{K}$ for a selected configuration, we derive a per-tile
mean prediction and a spatially resolved epistemic uncertainty map for each H\&E test
tile $x \in \mathcal{D}_{\text{test}}$.

\paragraph{Ensemble mean prediction.}
The ensemble mean image $\bar{y}(x)$ is computed as the pixel-wise arithmetic mean of
the ten generator outputs:
\begin{equation}
  \bar{y}(x) = \frac{1}{K} \sum_{k=1}^{K} G^{(k)}_{A \to B}(x).
\end{equation}
This mean image serves as the primary virtual stain prediction for each
ensemble-evaluated configuration and is used in all downstream metric computations
described in Section~\ref{sec:metrics}.

\paragraph{Epistemic uncertainty estimate.}
Epistemic uncertainty at each pixel location $i$ is estimated as the pixel-wise
variance across ensemble members:
\begin{equation}
  \sigma^2_c(x, y) = \frac{1}{K-1} \sum_{k=1}^{K}
    \bigl(G^{(k)}_{A \to B}(x)_{c,x,y} - \bar{y}(x)_{c,x,y}\bigr)^2,
    \quad c \in \{R, G, B\}.
\end{equation}
The three per-channel variance maps are summed to obtain a single-channel scalar
uncertainty map:
\begin{equation}
  U(x, y) = \sigma^2_R(x,y) + \sigma^2_G(x,y) + \sigma^2_B(x,y),
\end{equation}
giving $\mathbf{U}(x) \in \mathbb{R}^{H \times W}$, where $H{\times}W = 256{\times}256$
pixels.
Using the sample variance~($K{-}1$ denominator) is consistent with the unbiased
estimator of the population variance under the finite ensemble~\cite{lakshminarayanan2017simple}.
Summing rather than averaging channels preserves the total disagreement signal: a
pixel where all three channels disagree should register higher uncertainty than one
where only one channel varies.
High values in $\mathbf{U}(x)$ indicate pixels where the ensemble members disagree
substantially, reflecting regions where the model is uncertain about the correct
translation---for example, due to ambiguous texture, rare tissue morphologies, or
artefacts not well represented in the training distribution.

Tile-level scalar uncertainty is summarised as $\bar{\sigma}^2(x) =
\frac{1}{HW}\sum_i \sigma^2_i(x)$, the spatial mean of $\mathbf{U}(x)$, and used in
the correlation analyses of Section~\ref{sec:unc_vs_error}.


% -------------------------------------------------------
\subsection{Uncertainty Calibration}
\label{sec:unc_calibration}
% -------------------------------------------------------

A spatially resolved uncertainty map is only useful if it is \textit{calibrated}:
when the model assigns high uncertainty to a pixel, the translation should be
measurably worse at that pixel.
Verifying this requires an independent per-pixel error signal.
In a supervised setting this would be the absolute deviation from a known target;
in the unpaired setting of this work no such target exists, because the H\&E and SR
images originate from different physical sections.
We therefore use \textit{cycle-reconstruction error} as a proxy for per-pixel
translation difficulty.
MC~dropout~\cite{gal2016dropout} was not adopted as an alternative to deep ensembles
because its uncertainty estimates require extensive dropout-rate tuning to achieve
calibration, and miscalibration persists even after tuning in complex
tasks~\cite{siddiqui2024uncertainty,trustworthy_i2i2025}; deep ensembles yield
well-calibrated estimates with minimal hyperparameter
sensitivity~\cite{lakshminarayanan2017simple,siddiqui2024uncertainty}.

\paragraph{Error proxy: cycle-reconstruction error.}
For a translated tile $B' = G_{A \to B}(A)$, the cycle-reconstruction error at pixel
$(x, y)$ is defined as the absolute deviation between the input tile and its
cycle-reconstructed counterpart:
\begin{equation}
  E(x, y) = \bigl|A(x, y) - G_{B \to A}(B')(x, y)\bigr|,
\end{equation}
where $G_{B \to A}\colon B \to A$ is each model's own inverse generator applied to its
forward translation $B'$.
Since all six model families train paired forward and inverse generators, the self-cycle
is available without any external component.
Calibration results are stated as ``calibration with respect to cycle-reconstruction
error'' rather than against true translation error, which is unavailable in this
unpaired setting.

\paragraph{Tile preparation and tissue masking.}
Calibration is computed on a version of the test set re-tiled with zero overlap
(stride equal to tile size, $256{\times}256$ pixels), avoiding the double-counting
of border pixels that would bias across-tile and dataset-level statistics under
overlapping tiling.
All per-pixel metrics are restricted to tissue pixels identified by the preprocessing
tissue mask; background pixels --- where both $U$ and $E$ are near-zero --- are
excluded to prevent spurious inflation of rank correlations.

\paragraph{Calibration metrics.}
Once each tile yields a paired map $(U(x,y),\, E(x,y))$ over tissue pixels, we
evaluate calibration along three complementary dimensions following established
practice in predictive uncertainty
evaluation~\cite{kendall2017uncertainties}.

\subparagraph{Within-tile Spearman $\rho$ (spatial calibration).}
For each tile, $U$ and $E$ are flattened over tissue pixels and their Spearman rank
correlation $\rho$ is computed.
A value near $+1$ indicates that high-uncertainty pixels co-locate with high-error
pixels; near $0$ means the spatial structure of uncertainty is uninformative; a
negative value indicates anti-calibration.
We report mean~$\pm$~std of $\rho$ across all test tiles as the headline spatial
calibration figure.
Spearman rank correlation is preferred over Pearson because $U$ and $E$ are on
different scales and only their ordinal relationship is of interest.

\subparagraph{Across-tile Spearman and Pearson $\rho$ (tile-level calibration).}
Each tile is reduced to two scalars --- $\overline{U}$ and $\overline{E}$, the spatial
means over tissue pixels --- and both Spearman and Pearson correlations are computed
across all tiles.
This catches the failure mode where a model is spatially well-calibrated within each
tile but assigns identical mean uncertainty everywhere, making it useless for
flagging unreliable tiles for pathologist review.
Both coefficients are reported because tile-mean values tend to be more linearly
related than per-pixel values.

\subparagraph{ECE and reliability diagram (pixel-level and tile-level).}
Calibration is evaluated at two spatial scales using the same normalisation scheme:
both $U$ and $E$ are jointly rescaled to $[0,1]$ using dataset-global
$p_1$--$p_{99}$ bounds, making the diagonal of the reliability diagram a meaningful
reference and ECE scores comparable across architectures within this dataset.

\textit{Pixel-level.}
All tissue pixels across the test set are pooled and binned by $U$-quantile into
$B{=}10$ equal-mass bins.
Within each bin $b$, the mean normalised uncertainty $\overline{U_n}_b$ and mean
normalised error $\overline{E_n}_b$ are computed and plotted against each other;
perfect calibration corresponds to the identity diagonal.
The pixel-level ECE quantifies the weighted deviation from the
diagonal~\cite{guo2017ece}:
\begin{equation}
  \mathrm{ECE}_{\mathrm{pixel}} = \sum_{b=1}^{B} \frac{n_b}{N_{\mathrm{px}}}
    \bigl|\overline{U_n}_b - \overline{E_n}_b\bigr|,
\end{equation}
where $n_b$ is the number of pixels in bin $b$ and $N_{\mathrm{px}}$ is the total
number of tissue pixels.

\textit{Tile-level.}
Each tile is represented by two scalars — $\overline{U_n}$ and $\overline{E_n}$, the
spatial means of the normalised maps over tissue pixels — and tiles are binned by
$\overline{U_n}$-quantile into $B{=}10$ equal-mass bins.
The tile-level reliability diagram and ECE are constructed analogously:
\begin{equation}
  \mathrm{ECE}_{\mathrm{tile}} = \sum_{b=1}^{B} \frac{m_b}{M}
    \bigl|\overline{U_n}_b - \overline{E_n}_b\bigr|,
\end{equation}
where $m_b$ is the number of tiles in bin $b$ and $M$ is the total number of test
tiles.
The tile-level diagram catches the failure mode where a model appears pixel-calibrated
within each tile but assigns similar mean uncertainty to all tiles regardless of
their overall error level, making it unable to flag unreliable tiles for pathologist
review.
This construction adapts the classification ECE of~\cite{guo2017ece} to the
regression setting; values should not be compared directly to ECE from classification
contexts.

\paragraph{Caveats.}
The three calibration metrics characterise the same underlying property from
complementary angles and should be read as a converging picture rather than
independent evidence.
Cycle-reconstruction error is a proxy, not a ground truth: self-cycle error can be
underestimated when the forward and inverse generators share systematic biases, since
both directions may ignore the same tissue feature and the round trip still reconstructs
the source faithfully despite a poor forward translation.
Additionally, cross-section biological differences between the H\&E and SR sections add
an irreducible noise floor to ECE that no model can overcome.
These limitations are discussed further in Section~\ref{sec:discussion}.

% -------------------------------------------------------
\subsection{Results: Uncertainty Analysis}
\label{sec:unc_results}
\label{sec:unc_vs_error}
% -------------------------------------------------------

Results are reported for all six model families (CycleGAN, UNIT, MUNIT, DCLGAN,
UVCGAN, CycleDiffusion) using the best-performing generator size per family selected in
Section~\ref{sec:ens_selection}, each evaluated as a $K{=}10$ deep ensemble on the
held-out test set.
Within-tile Spearman $\rho$, across-tile Spearman and Pearson $\rho$,
$\mathrm{ECE}_{\mathrm{pixel}}$, and $\mathrm{ECE}_{\mathrm{tile}}$ are reported for
each model family.
\textcolor{red}{TODO: Figure --- overview panel per model family showing the reliability
diagrams (pixel-level and tile-level) and the distribution of within-tile Spearman
$\rho$ across test tiles.}

% -------------------------------------------------------
\subsubsection{Spatial Calibration}
\label{sec:unc_results_spatial}

Within-tile Spearman $\rho$ measures whether high-uncertainty pixels co-locate with
high cycle-reconstruction error pixels within each tile; a value near $+1$ indicates
well-calibrated spatial uncertainty structure.

\paragraph{CycleGAN.}
\textcolor{red}{[TODO: report mean $\pm$ std of within-tile Spearman $\rho$; note
whether spatial uncertainty tracks reconstruction error consistently across tiles.]}

\paragraph{UNIT.}
\textcolor{red}{[TODO]}

\paragraph{MUNIT.}
\textcolor{red}{[TODO]}

\paragraph{DCLGAN.}
\textcolor{red}{[TODO]}

\paragraph{UVCGAN.}
\textcolor{red}{[TODO]}

\paragraph{CycleDiffusion.}
\textcolor{red}{[TODO]}

\paragraph{Cross-model observation.}
\textcolor{red}{[TODO: compare mean within-tile $\rho$ across all six families; note
whether GAN-based and diffusion-based models differ in spatial calibration quality.]}

% -------------------------------------------------------
\subsubsection{Tile-level Calibration}
\label{sec:unc_results_tile}

Across-tile Spearman and Pearson $\rho$ and $\mathrm{ECE}_{\mathrm{tile}}$ measure
whether tiles with higher mean uncertainty correspond to tiles with higher mean
cycle-reconstruction error; a well-calibrated model should be usable for flagging
unreliable tiles for pathologist review.

\paragraph{CycleGAN.}
\textcolor{red}{[TODO: report across-tile Spearman $\rho$, Pearson $r$, and
$\mathrm{ECE}_{\mathrm{tile}}$; note whether mean tile uncertainty reliably predicts
tile-level error.]}

\paragraph{UNIT.}
\textcolor{red}{[TODO]}

\paragraph{MUNIT.}
\textcolor{red}{[TODO]}

\paragraph{DCLGAN.}
\textcolor{red}{[TODO]}

\paragraph{UVCGAN.}
\textcolor{red}{[TODO]}

\paragraph{CycleDiffusion.}
\textcolor{red}{[TODO]}

\paragraph{Cross-model observation.}
\textcolor{red}{[TODO: compare tile-level calibration across families; note whether
any architecture produces uncertainty estimates that are consistently miscalibrated at
the tile level despite reasonable spatial calibration.]}

% -------------------------------------------------------
\subsubsection{Calibration Shape}
\label{sec:unc_results_shape}

Pixel-level reliability diagrams and $\mathrm{ECE}_{\mathrm{pixel}}$ characterise
whether the \textit{magnitude} of uncertainty tracks the magnitude of error; deviation
above the diagonal indicates over-confidence, below indicates under-confidence.

\paragraph{CycleGAN.}
\textcolor{red}{[TODO: report $\mathrm{ECE}_{\mathrm{pixel}}$ and describe the shape of
the reliability diagram; note any systematic over- or under-confidence.]}

\paragraph{UNIT.}
\textcolor{red}{[TODO]}

\paragraph{MUNIT.}
\textcolor{red}{[TODO]}

\paragraph{DCLGAN.}
\textcolor{red}{[TODO]}

\paragraph{UVCGAN.}
\textcolor{red}{[TODO]}

\paragraph{CycleDiffusion.}
\textcolor{red}{[TODO]}

\paragraph{Cross-model observation.}
\textcolor{red}{[TODO: compare $\mathrm{ECE}_{\mathrm{pixel}}$ across all six families;
identify whether model families differ systematically in calibration shape and discuss
in relation to architectural differences.]}

% ============================================================
%  New bibliography entries for this section
%  (merge with references.bib)
% ============================================================
%
% @article{trustworthy_cell_ensemble2023,
%   author  = {Rauber, Paul and others},
%   title   = {Trustworthy in silico cell labeling via ensemble-based image translation},
%   journal = {Nature Communications},
%   year    = {2023},
%   note    = {PMC10663640},
%   url     = {https://pmc.ncbi.nlm.nih.gov/articles/PMC10663640/}
% }
%
% (Shared entries already listed:
%  lakshminarayanan2017simple, gal2016dropout, trustworthy_i2i2025)
